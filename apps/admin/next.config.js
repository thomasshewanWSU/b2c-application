/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "plus.unsplash.com",
      "images.unsplash.com",
      "m.media-amazon.com",
    ],
  },
};

export default nextConfig;
